ändere den zeitstempel auf sekundengenau

zeitstempel wie er auch in der Liste ANGEZEIGT werden soll: TT.MM.JJJJ, StundeStunde:MinuteMinute:Sekunde:Sekunde

ja nicht der name, das format, der name soll zeitstempel bleiben

mir wird aber erstens im feld immer noch genau das selbe format angezeigt und der name ist auch falsch

ok der name wurde geändert aber das feld nicht warum? überlege genau

ja soll es aber nicht das format soll ja geändert werden damit zeitpunkte sekundengenau hinterlegt werden können

immernoch nicht die eingabe kann maximal minutengenau erfolgen, löse das problem!

ja aber es soll doch sekundengenau erfolgen

hier immernoch nur minutengenau

ok warum geht es nicht gibt es keinen anderen zeitstemperl zur auswahl oder was? erkläre

nein, es ist nicht sekundengenau, nur minutengenau, du sollst erklären warum es nicht sekundengenau geht

ja warum wird sie nicht unterstützt hier im chat erklären

nenne den zeitstempel vom namen her wieder in nur zeitstempel um

füge direkt nach dem zeitstempel das feld Sekunde hinzu